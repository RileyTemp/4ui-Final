package Final;

import javax.swing.*;
import java.awt.*;

public class PanExponential extends JPanel {

    public PanExponential() {
        setLayout(new BorderLayout());
        JLabel background = new JLabel(new ImageIcon("exponential.png"));
        add(background);
    }
}
